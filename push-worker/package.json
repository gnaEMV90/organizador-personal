{
  "name": "planorha-push-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/schedule-core.js",
    "dev": "wrangler dev --test-scheduled",
    "migrate": "wrangler d1 execute planorha-db --remote --file=../migrations/0002_push_notifications.sql",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "web-push": "3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
