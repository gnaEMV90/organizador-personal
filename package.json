{
  "name": "planorha",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check bootstrap.js && node --check productivity-core.js && node --check manual-order-addon.js && node --check sync-core.js && node --check sw.js && node --check functions/api/state.js",
    "test": "node --test tests/*.test.mjs"
  }
}
